fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
asyncpg==0.30.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
httpx==0.28.1
apscheduler==3.11.0
google-genai>=1.0.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.149.0
cryptography==44.0.0
slowapi==0.1.9
python-dotenv==1.0.1
# Local, no-paid-API web search + scraping for the web agent (services.local_browser).
# Playwright drives a headless Chromium installed on the host (run
# `python -m playwright install chromium` after pip install). BeautifulSoup +
# markdownify clean scraped HTML into agent-ready text/markdown. All imported
# lazily, so the app still runs if these aren't installed (httpx+stub fallback).
playwright==1.49.1
beautifulsoup4==4.12.3
markdownify==0.13.1
# Tavily — agent-grade search API used as the PRIMARY web-search provider when
# TAVILY_API_KEY is set (no browser needed; reliable on Render). Imported lazily
# in services.local_browser so the app still runs without the package or a key.
tavily-python==0.5.0
